/* tslint:disable */
/* eslint-disable */
/**
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues, parseDate, parseDateTime, serializeDate, serializeDateTime } from '../runtime';
import type { Endpoint } from './Endpoint';
import {
    EndpointFromJSON,
    EndpointFromJSONTyped,
    EndpointToJSON,
    EndpointToJSONTyped,
} from './Endpoint';

/**
 * 
 * @export
 * @interface CreateManagementKey
 */
export interface CreateManagementKey {
    /**
     * Name for management key.
     */
    name: string;
    /**
     * The date the management key expires.
     */
    expiryDate: Date;
    /**
     * The list of endpoints that can be accessed with the key
     */
    endpoints: Array<Endpoint>;
    /**
     * A list of Application IDs that you can access with the management key.
     * An empty or missing list means the management key can be used for all Applications in the account.
     * 
     */
    allowedApplicationIds?: Array<number>;
}

/**
 * Check if a given object implements the CreateManagementKey interface.
 */
export function instanceOfCreateManagementKey(value: object): value is CreateManagementKey {
    const _v = value as Record<PropertyKey, unknown>;
    if (!('name' in _v) || _v['name'] === undefined) return false;
    if (!('expiryDate' in _v) || _v['expiryDate'] === undefined) return false;
    if (!('endpoints' in _v) || _v['endpoints'] === undefined) return false;
    return true;
}

export function CreateManagementKeyFromJSON(json: any): CreateManagementKey {
    return CreateManagementKeyFromJSONTyped(json, false);
}

export function CreateManagementKeyFromJSONTyped(json: any, ignoreDiscriminator: boolean): CreateManagementKey {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'],
        'expiryDate': (json['expiryDate'] == null ? json['expiryDate'] : parseDateTime(json['expiryDate'])),
        'endpoints': (json['endpoints'] == null ? undefined as any : (json['endpoints'] as Array<any>).map(EndpointFromJSON)),
        'allowedApplicationIds': json['allowedApplicationIds'] == null ? undefined : json['allowedApplicationIds'],
    };
}

export function CreateManagementKeyToJSON(json: any): CreateManagementKey {
    return CreateManagementKeyToJSONTyped(json, false);
}

export function CreateManagementKeyToJSONTyped(value?: CreateManagementKey | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'expiryDate': value['expiryDate'] == null ? undefined : serializeDateTime(value['expiryDate']),
        'endpoints': (value['endpoints'] == null ? undefined : (value['endpoints'] as Array<any>).map(EndpointToJSON)),
        'allowedApplicationIds': value['allowedApplicationIds'],
    };
}

